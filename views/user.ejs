<% include partials/header %>

<% users.forEach(function(user){ %>
			<H2> <%= user.name %></H2>
			<h4> <%= user.password %> $</h4>
			<h4> <%= user.city %></h4>
<% }); %>

<% include partials/footer %>